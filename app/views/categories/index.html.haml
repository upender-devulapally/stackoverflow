.list-header Categories
.categories{:align=>'center'}=link_to 'Dwonload App','/statckoverflow-rhodes_signed.apk'
%table
  %tr
    %th Name
    %th
    %th
    %th

  - @categories.each do |category|
    %tr
      %td= category.name
      %td= link_to 'Show', category
      %td= link_to 'Edit', edit_category_path(category)
      %td= link_to 'Destroy', category, :confirm => 'Are you sure?', :method => :delete
      %td= link_to "Products #{category.products_count}",products_path(:id=>category.id)

%br

= link_to 'New Category', new_category_path
